---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { ButtonLink } from "@/components/button-link";
import { breadcrumbJsonLd } from "@/components/json-ld";
import { learnGuideSlugs, learnGuides } from "@/data/learn-guides";
import {
  layaVsJevGuideSlugs,
  layaVsJevGuides,
  layaVsJevHub,
  layaVsJevGuidePath,
} from "@/data/laya-vs-jev-guides";
import { learnIndexPageSeo } from "@/lib/seo";
import { siteConfig } from "@/lib/site";

const seo = learnIndexPageSeo();

const topics = learnGuideSlugs.map((slug) => {
  const g = learnGuides[slug];
  return { href: `/learn/${slug}`, title: g.title, description: g.description };
});

const layaHubCards = layaVsJevGuideSlugs.map((slug) => {
  const g = layaVsJevGuides[slug];
  return { href: layaVsJevGuidePath(slug), title: g.title, description: g.tagline };
});
---

<BaseLayout
  title={seo.title}
  description={seo.description}
  path="/learn"
  ogImagePath={seo.imagePath}
  jsonLd={breadcrumbJsonLd([
    { name: "Home", path: "/" },
    { name: "Learn", path: "/learn" },
  ])}
>
  <div class="max-w-6xl page-shell">
    <header class="max-w-2xl">
      <h1 class="font-heading text-3xl font-semibold sm:text-4xl">Learn Jev</h1>
      <p class="mt-3 leading-relaxed text-muted-foreground">
        Educational hubs for System One in plain English. Meet <strong class="text-foreground">Choice</strong>, <strong class="text-foreground">Score</strong>, and <strong class="text-foreground">Noul</strong>, then jump to gateway wiring and comparisons like Laya vs Jev. Agent tool setup lives on
        <a href="/guides" class="text-primary hover:underline">/guides</a>.
      </p>
    </header>
    <section class="mt-10 grid gap-4 md:grid-cols-3">
      <div class="rounded-md border border-border/60 p-6">
        <h2 class="font-mono text-sm text-accent">choice</h2>
        <p class="mt-2 text-sm text-muted-foreground">Pick one label from a list. You get choice, probabilities, and confidence.</p>
      </div>
      <div class="rounded-md border border-border/60 p-6">
        <h2 class="font-mono text-sm text-accent">score</h2>
        <p class="mt-2 text-sm text-muted-foreground">Rate state on a rubric. You get score, probabilities, confidence.</p>
      </div>
      <div class="rounded-md border border-border/60 p-6">
        <h2 class="font-mono text-sm text-accent">noul</h2>
        <p class="mt-2 text-sm text-muted-foreground">Is this statement true? You get a noul value from 0 to 1.</p>
      </div>
    </section>
    <section class="mt-12 rounded-md border border-primary/30 bg-primary/5 p-6 sm:p-8">
      <h2 class="font-heading text-xl font-semibold">{layaVsJevHub.title}</h2>
      <p class="mt-2 max-w-2xl text-sm leading-relaxed text-muted-foreground">
        {layaVsJevHub.tagline} Independent comparison hub while open-weight Laya is trending: checkpoints, Laya-MLX on Mac, benchmark fairness, and hybrid patterns with hosted Jev.
      </p>
      <div class="mt-4 flex flex-wrap gap-3">
        <ButtonLink href="/learn/laya-vs-jev">Open hub</ButtonLink>
        <ButtonLink href={layaVsJevGuidePath("compare")} variant="outline">Comparison overview</ButtonLink>
        <ButtonLink href="/showcase?demo=laya-mlx-lonely-mh" variant="outline">laya-mlx snake demo</ButtonLink>
      </div>
      <ul class="mt-6 grid gap-3 sm:grid-cols-2">
        {layaHubCards.slice(0, 4).map((t) => (
          <li>
            <a
              href={t.href}
              class="block rounded-md border border-border/60 bg-background/60 p-4 transition-colors hover:border-primary/40 hover:bg-primary/5"
            >
              <p class="text-sm font-medium">{t.title}</p>
              <p class="mt-1 text-xs text-muted-foreground line-clamp-2">{t.description}</p>
            </a>
          </li>
        ))}
      </ul>
    </section>
    <section class="mt-12 rounded-md border border-border/60 bg-muted/20 p-6 sm:p-8">
      <h2 class="font-heading text-xl font-semibold">Agent setup guides</h2>
      <p class="mt-2 max-w-2xl text-sm leading-relaxed text-muted-foreground">
        LangChain, Cursor, Copilot, and other how-tos are on the guides hub, separate from these primers.
      </p>
      <div class="mt-4">
        <ButtonLink href="/guides/jev-with-ai-agents">Open agent guides hub</ButtonLink>
      </div>
    </section>
    <section class="mt-12">
      <h2 class="font-heading text-xl font-semibold">Guides</h2>
      <ul class="mt-6 grid gap-4 sm:grid-cols-2">
        {topics.map((t) => (
          <li>
            <a href={t.href} class="block rounded-md border border-border/60 p-5 transition-colors hover:border-primary/40 hover:bg-primary/5">
              <p class="font-medium">{t.title}</p>
              <p class="mt-1 text-sm text-muted-foreground">{t.description}</p>
            </a>
          </li>
        ))}
      </ul>
    </section>
    <section class="mt-12 flex flex-wrap gap-3">
      <ButtonLink href={siteConfig.typesafe.docs} external>Official TypeSafe docs</ButtonLink>
      <ButtonLink href={siteConfig.typesafe.playground} variant="outline" external>Playground</ButtonLink>
      <ButtonLink href={siteConfig.vercelGateway} variant="outline" external>Vercel AI Gateway (Jev)</ButtonLink>
      <ButtonLink href="/#home-faq-heading" variant="ghost">FAQ on home</ButtonLink>
    </section>
  </div>
</BaseLayout>
